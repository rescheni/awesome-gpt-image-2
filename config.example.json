{
  "_comment": "复制为 config.json 后修改。此文件不含真实凭据, 可安全提交。",
  "secret_key": "请填入随机字符串(可用 python3 -c \"import secrets;print(secrets.token_hex(32))\")",
  "users": {
    "admin": "改成你自己的强密码"
  },
  "llm": {
    "enabled": false,
    "base_url": "https://api.openai.com/v1",
    "model": "gpt-4o",
    "api_key": "sk-..."
  }
}
